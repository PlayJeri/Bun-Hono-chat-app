{
  "scripts": {
    "start": "ENV=integrated bun run --hot src/index.ts",
    "dev": "ENV=dev bun run --hot src/index.ts",
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.1.11",
    "@libsql/client": "^0.9.0",
    "bun": "^1.0.30",
    "drizzle-orm": "^0.33.0",
    "hono": "latest",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/jsonwebtoken": "^9.0.6",
    "drizzle-kit": "^0.24.0"
  }
}
